export enum AppRoute {
  Login= '/login',
  Profile = '/',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN'
}

export enum APIRoutes {
  Orders = '/orders',
  Login = '/users/login',
  Facades = '/facades'
}